<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.disasterawarerouting">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_launcher"
        android:roundIcon="@drawable/ic_app_launcher"
        android:label="PathGuard"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- SPLASH SCREEN -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN SCREEN -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- SIGN UP SCREEN -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- MAIN SCREEN -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- EMERGENCY CONTACTS SCREEN (MISSING EARLIER) -->
        <activity
            android:name=".EmergencyContactsActivity"
            android:exported="false" />

        <!-- CUSTOM SAFETY MAP TRACKER -->
        <activity
            android:name=".MapTrackerActivity"
            android:exported="false" />

    </application>

</manifest>